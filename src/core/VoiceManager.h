#pragma once

// Placeholder for voice management interface
class VoiceManager
{
public:
    VoiceManager() = default;
    ~VoiceManager() = default;
    
    // Voice allocation methods will be implemented here
};